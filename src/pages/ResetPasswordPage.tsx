export default function ResetPasswordPage() {
  return (
    <div className="bg-background flex h-screen items-center justify-center">
      <p className="text-muted-foreground text-sm">Reset Password — coming in Module C</p>
    </div>
  );
}
