export default function LoginPage() {
  return (
    <div className="bg-background flex h-screen items-center justify-center">
      <p className="text-muted-foreground text-sm">Login — coming in Module B</p>
    </div>
  );
}
